diff --git a/configs/ppyoloe/ppyolo_custom_s.py b/configs/ppyoloe/ppyolo_custom_s.py
new file mode 100644
index 0000000..ef7d590
--- /dev/null
+++ b/configs/ppyoloe/ppyolo_custom_s.py
@@ -0,0 +1,116 @@
+_base_ = '/home/computador/Desktop/models_research/mmyolo/configs/ppyoloe/ppyoloe_plus_s_fast_8xb8-80e_coco.py'
+
+
+
+# Dataset root
+data_root = '/home/computador/Desktop/models_research/mmyolo/Dataset/'
+class_name = ('Seal', 'Tag_White', 'Tag_Yellow')
+num_classes = len(class_name)
+metainfo = dict(classes=class_name)
+
+# Dataset metadata
+#metainfo = dict(classes=class_name, palette=[(255, 0, 0), (0, 255, 0), (0, 0, 255)])
+
+# 🔧 Model config with P2 enabled
+model = dict(
+    backbone=dict(
+        type='PPYOLOECSPResNet',
+        widen_factor = 1.0,
+        out_indices=(1, 2, 3, 4),   # >>> include C2
+    ),
+    neck=dict(
+        type='PPYOLOECSPPAFPN',
+        in_channels=[128,256,512,1024],   # >>> 4 stages (C2–C5)
+        out_channels=[128,256,512,1024],  # >>> match channels
+
+    ),
+    bbox_head=dict(
+        type='PPYOLOEHead',
+        head_module=dict(
+            num_classes=num_classes,
+         
+            featmap_strides=[4, 8, 16, 32],# >>> add stride=4
+            in_channels=[128,256,512,1024], # >>> match neck outputs
+            #widen_factor=1.0,                 # >>> ensure no channel scaling
+    
+        ),
+    ),
+    train_cfg=dict(
+        initial_assigner=dict(num_classes=num_classes),
+        assigner=dict(num_classes=num_classes)
+    )
+)
+
+# 🔧 Dataloaders
+train_dataloader = dict(
+    batch_size=6,
+    num_workers=4,
+    dataset=dict(
+        type='YOLOv5CocoDataset',
+        data_root=data_root,
+        metainfo=metainfo,
+        ann_file='train/_annotations.coco.json',
+        data_prefix=dict(img='train/'),
+        # pipeline=[
+        #     dict(type='LoadImageFromFile'),
+        #     dict(type='LoadAnnotations', with_bbox=True),
+        #     dict(type='YOLOv5CocoDataset', ann_file='train/_annotations.coco.json')
+        # ]
+    )
+)
+
+val_dataloader = dict(
+    batch_size=6,
+    num_workers=4,
+    dataset=dict(
+        type='YOLOv5CocoDataset',
+        data_root=data_root,
+        metainfo=metainfo,
+        ann_file='valid/_annotations.coco.json',
+        data_prefix=dict(img='valid/')
+    )
+)
+
+
+test_dataloader = dict(
+    batch_size=6,
+    num_workers=4,
+    dataset=dict(
+        type='YOLOv5CocoDataset',
+        data_root=data_root,
+        metainfo=metainfo,
+        ann_file='test/_annotations.coco.json',   
+        data_prefix=dict(img='test/')       
+    )
+)
+
+# Evaluators
+
+val_evaluator = dict(ann_file=data_root + 'valid/_annotations.coco.json')
+test_evaluator = dict(ann_file=data_root + 'test/_annotations.coco.json')
+
+# Training schedule
+max_epochs = 100
+train_cfg = dict(max_epochs=max_epochs, val_interval=10)
+
+# Logger & checkpoints
+default_hooks = dict(
+    checkpoint=dict(interval=10, max_keep_ckpts=2, save_best='auto'),
+    logger=dict(type='LoggerHook', interval=5),
+    param_scheduler=dict(
+        warmup_min_iter=10,
+        warmup_epochs=3,
+        total_epochs=int(max_epochs * 1.2)
+    )
+)
+
+# Load pretrained weights (still use PP-YOLOE-S pretrained)
+load_from = 'https://download.openmmlab.com/mmyolo/v0/ppyoloe/ppyoloe_plus_s_fast_8xb8-80e_coco/ppyoloe_plus_s_fast_8xb8-80e_coco_20230101_154052-9fee7619.pth'
+
+visualizer = dict(
+    vis_backends=[dict(type='LocalVisBackend')]
+)
+
+optim_wrapper = dict(
+    optimizer=dict(type='SGD', lr=0.001, momentum=0.9, weight_decay=5e-4)
+)
diff --git a/configs/ppyoloe/ppyoloe_m_custom_seal.py b/configs/ppyoloe/ppyoloe_m_custom_seal.py
new file mode 100644
index 0000000..0ecfd0f
--- /dev/null
+++ b/configs/ppyoloe/ppyoloe_m_custom_seal.py
@@ -0,0 +1,104 @@
+_base_ = '/home/computador/Desktop/models_research/loss/mmyolo/configs/ppyoloe/ppyoloe_plus_s_fast_8xb8-80e_coco.py'
+
+
+# Dataset root
+data_root = '/home/computador/Desktop/models_research/mmyolo/Dataset/'
+class_name = ('Seal', 'Tag_White', 'Tag_Yellow')
+num_classes = len(class_name)
+metainfo = dict(classes=class_name)
+
+
+# Model config
+model = dict(
+    bbox_head=dict(
+        head_module=dict(num_classes=num_classes),
+    
+        loss_bbox=dict(
+            type='PIOULoss',
+            iou_mode='piou',       # for compatibility with loss builder
+            bbox_format='xyxy',    # PP-YOLOE uses xyxy internally
+            reduction='mean',
+            loss_weight=2.5,
+            return_iou=False
+        )
+    ),
+    train_cfg=dict(
+        initial_assigner=dict(num_classes=num_classes),
+        assigner=dict(num_classes=num_classes)
+    )
+)
+
+
+# 🔧 Dataloaders
+train_dataloader = dict(
+    batch_size=6,
+    num_workers=4,
+    dataset=dict(
+        type='YOLOv5CocoDataset',
+        data_root=data_root,
+        metainfo=metainfo,
+        ann_file='train/_annotations.coco.json',
+        data_prefix=dict(img='train/'),
+        # pipeline=[
+        #     dict(type='LoadImageFromFile'),
+        #     dict(type='LoadAnnotations', with_bbox=True),
+        #     dict(type='YOLOv5CocoDataset', ann_file='train/_annotations.coco.json')
+        # ]
+    )
+)
+
+val_dataloader = dict(
+    batch_size=6,
+    num_workers=4,
+    dataset=dict(
+        type='YOLOv5CocoDataset',
+        data_root=data_root,
+        metainfo=metainfo,
+        ann_file='valid/_annotations.coco.json',
+        data_prefix=dict(img='valid/')
+    )
+)
+
+
+test_dataloader = dict(
+    batch_size=6,
+    num_workers=4,
+    dataset=dict(
+        type='YOLOv5CocoDataset',
+        data_root=data_root,
+        metainfo=metainfo,
+        ann_file='test/_annotations.coco.json',   
+        data_prefix=dict(img='test/')       
+    )
+)
+
+# Evaluators
+
+val_evaluator = dict(ann_file=data_root + 'valid/_annotations.coco.json')
+test_evaluator = dict(ann_file=data_root + 'test/_annotations.coco.json')
+
+# Training schedule
+max_epochs = 100
+train_cfg = dict(max_epochs=max_epochs, val_interval=10)
+
+# Logger & checkpoints
+default_hooks = dict(
+    checkpoint=dict(interval=10, max_keep_ckpts=2, save_best='auto'),
+    logger=dict(type='LoggerHook', interval=5),
+    param_scheduler=dict(
+        warmup_min_iter=10,
+        warmup_epochs=3,
+        total_epochs=int(max_epochs * 1.2)
+    )
+)
+
+# Load pretrained weights (still use PP-YOLOE-S pretrained)
+load_from = 'https://download.openmmlab.com/mmyolo/v0/ppyoloe/ppyoloe_plus_s_fast_8xb8-80e_coco/ppyoloe_plus_s_fast_8xb8-80e_coco_20230101_154052-9fee7619.pth'
+
+visualizer = dict(
+    vis_backends=[dict(type='LocalVisBackend')]
+)
+
+optim_wrapper = dict(
+    optimizer=dict(type='SGD', lr=0.001, momentum=0.9, weight_decay=5e-4)
+)

diff --git a/mmyolo/models/losses/iou_loss.py b/mmyolo/models/losses/iou_loss.py
index e3d3dc4..de6f45d 100644
--- a/mmyolo/models/losses/iou_loss.py
+++ b/mmyolo/models/losses/iou_loss.py
@@ -44,7 +44,7 @@ def bbox_overlaps(pred: torch.Tensor,
     Returns:
         Tensor: shape (n, ).
     """
-    assert iou_mode in ('iou', 'ciou', 'giou', 'siou')
+    assert iou_mode in ('iou', 'ciou', 'giou', 'siou', 'piou')
     assert bbox_format in ('xyxy', 'xywh')
     if bbox_format == 'xywh':
         pred = HorizontalBoxes.cxcywh_to_xyxy(pred)
@@ -148,8 +148,31 @@ def bbox_overlaps(pred: torch.Tensor,
 
         ious = ious - ((distance_cost + shape_cost) * 0.5)
 
+    elif iou_mode == 'piou':
+        # PIoU: Perceptual IoU (corner-based), from SOD-YOLOv8
+        # Enhances robustness to partial occlusion and viewpoint variance
+
+        # Compute base IoU (already done above)
+        iou_base = ious.clamp(min=0.0)
+
+        # Corner distance penalty
+        corner_diff = ((bbox1_x1 - bbox2_x1)**2 + (bbox1_y1 - bbox2_y1)**2 +
+                       (bbox1_x2 - bbox2_x2)**2 + (bbox1_y2 - bbox2_y2)**2)
+
+        # Normalize by GT diagonal (to scale penalty)
+        diag = ((bbox2_x2 - bbox2_x1)**2 + (bbox2_y2 - bbox2_y1)**2).clamp(min=eps)
+
+        # Apply exponential decay on corner distance
+        corner_penalty = torch.exp(-corner_diff / diag)
+
+        # Compute final PIoU
+        ious = iou_base * corner_penalty
+
+
     return ious.clamp(min=-1.0, max=1.0)
 
+    
+
 
 @MODELS.register_module()
 class IoULoss(nn.Module):
diff --git a/mmyolo/models/losses/piou_loss.py b/mmyolo/models/losses/piou_loss.py
new file mode 100644
index 0000000..47d5306
--- /dev/null
+++ b/mmyolo/models/losses/piou_loss.py
@@ -0,0 +1,70 @@
+# Copyright (c) OpenMMLab. All rights reserved.
+import torch
+import torch.nn as nn
+from typing import Optional, Tuple, Union
+
+from mmdet.models.losses.utils import weight_reduce_loss
+from mmyolo.registry import MODELS
+from .iou_loss import bbox_overlaps  # reuse your bbox_overlaps
+
+@MODELS.register_module()
+class PIOULoss(nn.Module):
+    """PIoU Loss (Perceptual IoU, corner-based for occlusion robustness).
+
+    Args:
+        bbox_format (str): "xywh" or "xyxy". Defaults to "xywh".
+        eps (float): Numerical stability epsilon.
+        reduction (str): "none", "mean", or "sum".
+        loss_weight (float): Weight factor.
+        return_iou (bool): Return IoU tensor if True.
+    """
+
+    def __init__(self,
+                 iou_mode: str = 'piou',
+                 bbox_format: str = 'xywh',
+                 eps: float = 1e-7,
+                 reduction: str = 'mean',
+                 loss_weight: float = 1.0,
+                 return_iou: bool = True):
+        super().__init__()
+        assert bbox_format in ('xywh', 'xyxy')
+        self.bbox_format = bbox_format
+        self.eps = eps
+        self.reduction = reduction
+        self.loss_weight = loss_weight
+        self.return_iou = return_iou
+
+    def forward(
+        self,
+        pred: torch.Tensor,
+        target: torch.Tensor,
+        weight: Optional[torch.Tensor] = None,
+        avg_factor: Optional[float] = None,
+        reduction_override: Optional[Union[str, bool]] = None
+    ) -> Tuple[Union[torch.Tensor, torch.Tensor], torch.Tensor]:
+
+        if weight is not None and not torch.any(weight > 0):
+            if pred.dim() == weight.dim() + 1:
+                weight = weight.unsqueeze(1)
+            return (pred * weight).sum()
+
+        assert reduction_override in (None, 'none', 'mean', 'sum')
+        reduction = reduction_override if reduction_override else self.reduction
+
+        if weight is not None and weight.dim() > 1:
+            weight = weight.mean(-1)
+
+        iou = bbox_overlaps(
+            pred,
+            target,
+            iou_mode='piou',  # 👈 use new PIoU mode
+            bbox_format=self.bbox_format,
+            eps=self.eps)
+
+        loss = self.loss_weight * weight_reduce_loss(1.0 - iou, weight,
+                                                     reduction, avg_factor)
+
+        if self.return_iou:
+            return loss, iou
+        else:
+            return loss
diff --git a/myreadme.md b/myreadme.md
new file mode 100644
index 0000000..00dc259
--- /dev/null
+++ b/myreadme.md
@@ -0,0 +1,156 @@
+Below is a **clean, professional, production-quality README.md** for your lightweight custom MMYOLO modifications repository.
+
+I wrote it in a way that works **even if people don’t know MMYOLO**, fully explains patch usage, and looks good on GitHub.
+
+---
+
+# ⭐ **README.md — PP-YOLOE Custom Extension (Lightweight Patch Repository)**
+
+````markdown
+# 🚀 PP-YOLOE Custom Extension (Lightweight Patch Repository)
+
+This repository contains a **small and clean patch** that applies my custom modifications to the OpenMMLab **MMYOLO** implementation of **PP-YOLOE**.
+
+✔ No need to upload or fork the entire MMYOLO framework  
+✔ Fully compatible with MMEngine, MMDetection, and MMYOLO  
+✔ Easy to apply on any machine using a one-line patch command  
+
+---
+
+## 📌 What This Repository Contains
+
+This repo includes only:
+
+- `patches/my_custom_ppyoloe.patch`  
+  → A Git patch containing all my modifications  
+- `configs/ppyoloe_custom.py`  
+  → A configuration file for training the custom model  
+- `README.md`  
+  → Documentation (this file)
+
+This keeps the repository **lightweight**, **clean**, and **easy to maintain**.
+
+---
+
+## 🧩 Features of the Custom PP-YOLOE
+
+This patch includes modifications such as:
+
+- Custom backbone changes (e.g., modified CSPResNet)
+- Modified PP-YOLOE neck (e.g., PPAFPN / custom P2 layer support)
+- Architecture tweaks for small object detection
+- Custom channel sizes, out_indices, or depth changes
+- Additional model-specific improvements
+
+> ⚠️ The exact changes depend on your patch.  
+This repo provides the patch so others can apply them to official MMYOLO.
+
+---
+
+## 📥 1. Install MMYOLO (Official)
+
+```bash
+git clone https://github.com/open-mmlab/mmyolo
+cd mmyolo
+pip install -r requirements/build.txt
+pip install -v -e .
+````
+
+---
+
+## 🔧 2. Apply the Custom Patch
+
+Clone this repo **outside** the MMYOLO folder:
+
+```bash
+git clone https://github.com/<your-username>/my-ppyoloe-custom.git
+```
+
+Apply patch:
+
+```bash
+cd mmyolo
+git apply ../my-ppyoloe-custom/patches/my_custom_ppyoloe.patch
+```
+
+If there is no output → the patch applied *successfully*.
+
+---
+
+## ⚙️ 3. Train Using the Custom Config
+
+```bash
+python tools/train.py ../my-ppyoloe-custom/configs/ppyoloe_custom.py
+```
+
+---
+
+## 📂 Project Structure
+
+```
+my-ppyoloe-custom/
+│
+├── patches/
+│   └── my_custom_ppyoloe.patch
+│
+├── configs/
+│   └── ppyoloe_custom.py
+│
+└── README.md
+```
+
+---
+
+## 🔄 Updating Your Patch (If You Add New Changes)
+
+Whenever you modify MMYOLO again, generate a new patch:
+
+```bash
+git diff base/master master > patches/my_custom_ppyoloe.patch
+```
+
+Then push:
+
+```bash
+git add .
+git commit -m "updated patch"
+git push
+```
+
+---
+
+## 📝 Notes
+
+* No OpenMMLab code is distributed in this repo → **fully compliant** with licensing.
+* Patch works across MMYOLO versions as long as file structure hasn’t changed significantly.
+* Ideal for sharing custom architectures without forking the full repo.
+
+---
+
+## 🧑‍💻 Author
+
+**Your Name (manikanta)**
+If you use this in research, feel free to mention or credit.
+
+---
+
+## 🌟 Support & Contributions
+
+If you want to contribute improvements, feel free to open a pull request or issue.
+
+---
+
+# 🎉 Enjoy Training Your Custom PP-YOLOE!
+
+```
+
+---
+
+If you want, I can also generate:
+
+✅ A sample `ppyoloe_custom.py` config  
+✅ A `setup.cfg` or license file  
+✅ A project logo/header banner  
+
+Just tell me!
+```
